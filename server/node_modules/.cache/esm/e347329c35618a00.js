let express,readdirSync,cors,mongoose;_356‍.w("express",[["default",["express"],function(v){express=v}]]);_356‍.w("fs",[["readdirSync",["readdirSync"],function(v){readdirSync=v}]]);_356‍.w("cors",[["default",["cors"],function(v){cors=v}]]);_356‍.w("mongoose",[["default",["mongoose"],function(v){mongoose=v}]]);



const morgan = require("morgan");
require('dotenv').config();

const app = express();
//db connection
mongoose.connect(process.env.DATABASE).then(()=> console.log('DB Connected'))
.catch(() => _356‍.g.console.log('DB Connection Failed', err));

//middlewares
app.use(cors()); //make sure cors is always on top of the rest of the middlewares to avoid possible errors
app.use(morgan("dev"));
app.use(express.json());

//route middleware 
readdirSync("./routes").map((r) => app.use("/api", require(`./routes/${r}`)));

const port = process.env.PORT || 6000;

app.listen(port, () => console.log(`whats good this is the server on port ${port}`));